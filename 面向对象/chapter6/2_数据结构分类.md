# 数据结构分类：
    数据结构=存储、组织数据的方式，是算法解决问题的载体
## 线性结构
    特点：每个节点都只有一个前驱一个后继
### 顺序表 栈 队列
    元素顺序地存放在一块 连续的存储区里
#### 一站式存储 数据区 信息区
#### 分离式存储
    地址的大小为4字节是固定的，可以存储地址
#### 顺序表在获取数据时直接通过下标偏移就可以找到数据所在，无需遍历，所以顺序表在获取地址操作的时间复杂度为O(1)
### 链表
## 非线性结构
    特点：每个节点都有N个前驱N个后继
### 树 图